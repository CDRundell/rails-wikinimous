<ul style="list-style-type:none">
<% @articles.each do |article|%>
  <li>
  🍬 <%= link_to article[:title], article_path(article[:id])%><%= link_to "🗑", article_path(article), method: :delete, data: { confirm: "Are you sure?" } %>
  </li>
<% end %>
</ul>

<strong><%= link_to "Add new task", new_article_path %></strong>
